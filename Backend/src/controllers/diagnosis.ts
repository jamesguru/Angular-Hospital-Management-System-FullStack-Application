import { Request, Response } from "express";

export const createDiagnosis = (req:Request,res:Response) =>{
    res.json({message:"Hitted"})

}

export const updateDiagnosis =(req:Request,res:Response) =>{
    res.json({message:"Hitted"})
}

export const getDiagnosisForUser = (req:Request,res:Response) =>{

    res.json({message:"Hitted"})
}